.. highlight:: shell

============
Installation
============

At the command line::

    $ easy_install cashflow

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv cashflow
    $ pip install cashflow
